# SaaS Landing Page Builder

A drag-and-drop landing page builder where users create, customize, and publish pages using pre-built blocks.

Portfolio project — the goal is learning senior-level architecture, not just shipping code.

## Rules

See `.claude/rules/` for how we work together.

## Current state

See `docs/` for architecture and API docs, `ROADMAP.md` for phases and progress.
